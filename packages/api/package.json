{
  "name": "@ntm-connect/api",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "clean": "rimraf dist",
    "build": "pnpm clean && tsc",
    "knex": "node --import tsx/esm -e \"require('knex/bin/cli')\" knex --knexfile ../shared/src/knexfile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@hono/node-server": "^1.12.1",
    "@ntm-connect/shared": "workspace:../shared",
    "hono": "^4.5.9"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "tsx": "^4.18.0"
  }
}