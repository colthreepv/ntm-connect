# Use LLM to generate `storeId` based on `storeFullName`

## Prompt

```text
Can you generate for me a `storeId` for each store?
It should be a single or composite word, like "torrino" or "scurcola-marsicana"
```
